{
  "name": "fang",
  "version": "0.0.1",
  "description": "",
  "main": "index.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint -c .eslintrc src/fang.js",
    "preinstall": "npm i -g babel uglify-js",
    "prebuild": "npm run lint",
    "build": "babel src/fang.js > index.js",
    "postbuild": "npm run uglify",
    "uglify": "uglifyjs index.js > index.min.js"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "eslint": "^1.6.0"
  }
}
